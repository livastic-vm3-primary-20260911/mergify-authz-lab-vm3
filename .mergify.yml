merge_protections:
  # Migrated from post_check rule 'post-check-migration-probe'. Dropped options with no equivalent: title
  - name: post-check-migration-probe
    if:
      - label = vm3-migration-never
    success_conditions:
      - label = vm3-migration-never
