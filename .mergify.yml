commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

queue_rules:
  - name: vm3-app-selection-revocation
    queue_conditions: []
    merge_conditions:
      - check-success = vm3-ff-review-hold-v2
    merge_method: merge
    merge_bot_account: livastic-vm3-primary-20260911
    batch_size: 1

merge_queue:
  mode: serial
  max_parallel_checks: 1
