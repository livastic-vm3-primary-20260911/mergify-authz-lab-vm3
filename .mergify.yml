pull_request_rules:
  - name: dispatch-provenance-proof
    conditions:
      - label = vm3-dispatch-trigger
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: vm3-dispatch-provenance.yml
