commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

queue_rules:
  - name: vm3-two-step-proof
    queue_conditions:
      - base = vm3-two-step-target
    merge_conditions:
      - check-success = vm3-premerge-required
    merge_method: merge
    batch_size: 1

merge_queue:
  mode: serial
  max_parallel_checks: 1
