commands_restrictions:
  queue:
    conditions:
      - sender = livastic-vm3-primary-20260911

queue_rules:
  - name: vm3-app-revoke-time-gate
    queue_conditions:
      - base = vm3-app-revoke-target
    merge_conditions:
      - "#approved-reviews-by >= 1"
      - current-datetime >= 2026-09-13T09:31:47Z
    merge_method: fast-forward
    batch_size: 1

merge_queue:
  mode: serial
  max_parallel_checks: 1
